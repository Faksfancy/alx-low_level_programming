#include "main.h"

/**
 * print_numbers - check the code
 * Return: Always 0.
*/

void print_numbers(void)
{
	int n;

	if (n >= '48' && n <= '57')
	{
		_putchar(n);
		_putchar('\n');
	}
	else
	{
		return (0);
	}
}
